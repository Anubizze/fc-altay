Vercel

1. После распаковки (flatten.bat / flatten.sh) в корне лежат package.json, src/, next.config.mjs.
2. В настройках проекта Vercel поле Root Directory должно быть ПУСТЫМ (корень репозитория).
3. Если раньше было указано "frontend" — очистите и сделайте Redeploy.
